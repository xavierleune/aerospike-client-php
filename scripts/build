#!/bin/bash

if [ -f Makefile ]; then
  make clean
fi
phpize
./configure "CFLAGS=-g -O3" --enable-aerospike
make clean all CFLAGS=-g EXTRA_INCLUDES+=-I$CLIENTREPO_3X/include EXTRA_LDFLAGS="-L$CLIENTREPO_3X/lib -laerospike -llua -lrt"
